# 画布格式 · 多尺寸交付与安全区

> 什么时候读：**开工前定画布**（风格门问「交付格式」时同时定），以及**一处内容要多格式产出**时。
> 画布定错 = 字号体系、版式、图片槽位全部要重算，**别把它当成最后一步的设置项**。

---

## 一 · 格式速查

| ID | 格式 | 像素尺寸 | 比例 | 典型用途 |
|---|---|---|---|---|
| `ppt16x9` | PPT / 屏幕 16:9 | `1920 × 1080` | 16:9 | **默认**。投影、会议、路演 |
| `ppt4x3` | PPT 4:3 | `1024 × 768` | 4:3 | 老投影仪、部分学术报告厅 |
| `a4` | A4 打印 | `1240 × 1754` | 1:√2 | 讲义、传单、报告封面 |
| `xhs` | 小红书图文 | `1242 × 1660` | 3:4 | 图文笔记、知识卡 |
| `square` | 朋友圈 / IG 方图 | `1080 × 1080` | 1:1 | 方版海报、品牌展示 |
| `story` | Story / 短视频封面 | `1080 × 1920` | 9:16 | 竖屏故事、短视频封面 |
| `wechat` | 公众号头图 | `900 × 383` | 2.35:1 | 文章封面 |
| `banner` | 宽屏横幅 | `1920 × 1080` | 16:9 | 网页 banner、数字屏 |

**同比例 ≠ 同坐标系**：`banner` 与 `ppt16x9` 都是 16:9，但坐标基准可以不同。
**一个 deck 内所有页必须共享同一个画布数值**——不要逐页改尺寸。

**自定义画布**用正整数值；分数尺寸只在「导入的 PPT 原始尺寸」这种兼容场景接受。
可用的幻灯片边长范围约 `96–5376` SVG px（对应 EMU 914400–51206400）。

---

## 二 · 单位与换算（唯一正确的那套）

四条线之间换算最常出错，记死这几个锚点：

```
1 inch = 72 pt = 914400 EMU
基准画布 16:9  ⇔  960 pt × 540 pt  ⇔  13.333″ × 7.5″  ⇔  12192000 × 6858000 EMU
                 ⇔  1920 px × 1080 px （取 1 px = 0.5 pt = 6350 EMU）
```

| 从 → 到 | 系数 |
|---|---|
| px → pt | `× 0.5` |
| pt → EMU | `× 12700` |
| px → EMU | `× 6350` |
| pt → inch | `÷ 72` |

> ⚠️ **不要用 96 DPI 那套 `1 px = 0.75 pt`** —— 整个 deck 会缩到 2/3。
> 这条在 `27-pptx-assembly.md` 与 `10-drive-engine.md` 各踩过一次，这里是第三处提醒。

**各线的画布约定**：

| 线 | 画布 | 说明 |
|---|---|---|
| 手作线 | SVG 用 `1920 × 1080`（或对应格式像素），导出时按上表映射 | 见 `08-page-craft.md` |
| 量产线 | `deck_data.json` 的 `canvas` 固定 `960 × 540`（pt），坐标即 pt | 见 `11-deck-data-format.md` |
| 网页线 | CSS 用 `vw/vh` 或 `100dvh` 自适应，不写死 px | 见 `25-web-deck.md` |
| 原生编辑 | 保持源 PPTX 原尺寸，**不改画布** | 见 `13-edit-existing.md` |

---

## 三 · 字号随画布缩放（不能照搬）

**规则：以 16:9（短边 540 pt）为基准，其它画布的字号按「短边比」等比缩放。**

```
新字号 = 基准字号 × (新画布短边 / 540 pt)
```

举例：基准正文 24pt 投在 16:9 上是合适的；换到 4:3（短边 768px = 384pt）→ `24 × 384/540 ≈ 17pt`；
换到竖版 9:16（短边 1080px = 540pt）→ 保持 24pt（短边恰好也是 540pt）。

**各格式的最小正文字号（经验值）**：

| 格式 | 阅读距离 | 最小正文 | 说明 |
|---|---|---|---|
| `ppt16x9` | 远（投影） | **20–24 pt** | 投屏硬下限，`22-style-minimal.md` 有字号阶梯 |
| `ppt4x3` | 远 | 18–20 pt | 按短边缩放后的结果 |
| `a4` | 近（手持） | **10–12 pt** | 打印用，可以小 |
| `xhs` / `square` | 近（手机） | 正文 ≈ **画布宽度 ÷ 55**；标题可到 ÷ 8 | 手机上看的是「一屏一眼」 |
| `story` | 近（手机） | 比 `xhs` 再大 15–20% | 竖屏滑动快，字要大 |
| `wechat` | 近（列表缩略） | 极简：**只留一个标题** | 2.35:1 太窄，正文必糊 |

**标题字号的双约束**（网页线与 SVG 都适用）：`min(Xvw, Yvh)`，且 **Y ≥ X × 1.6**。
只锁 vw 会在矮屏溢出，只锁 vh 会在宽屏显得小（`29-preflight-checklist.md` P5）。

---

## 四 · 平台安全区（竖版最容易翻车）

画面「有内容」不等于「用户看得见」——平台 UI 会盖住画面边缘。

| 格式 | 必须避开的区域 | 安全做法 |
|---|---|---|
| `story` 9:16 | 底部 **≈ 14–18%**（进度条/操作区）、顶部 ≈ 8%（返回/账号） | 关键信息压在**中间 70%** |
| `xhs` 3:4 | 底部 ≈ 10%、右上角（收藏/分享） | 右下角不放重要元素 |
| `square` 1:1 | 四周各 ≈ 6% | 主体居中 |
| `wechat` 2.35:1 | **左右各 ≈ 8%**（列表裁剪时的裁切区） | 标题居中，不做满宽 |
| `ppt16x9` | 投屏时四周 ≈ 5%（投影裁边） | 与「画布对齐法则」的边界一致 |

**通用做法**：给画布加一层「安全区参考框」在开发期可见（`outline` 描边），交付前关掉。
参考框边界 = `29-preflight-checklist.md` 的越界判据。

---

## 五 · 一处内容，多格式产出

同一份内容要出多种格式（例：16:9 讲 + 3:4 发笔记）时：

1. **不要缩放同一份 HTML/PPTX**——比例不同，版式必须重排，缩出来必然「两边都不对」
2. **共享内容层，重写版式层**：标题层级、要点、数据、图片这些内容不变；
   分栏数、字号、图片槽位比例按新画布重定
3. **顺序**：先定**主格式**（通常是 16:9）做完整套，再从它派生其他格式
4. **图片**：不同比例要**按目标槽位取/生成**（`28-image-prompts.md` §二），不要把 16:9 硬裁成 9:16

| 场景 | 主格式 | 派生 | 注意 |
|---|---|---|---|
| 演讲 + 社媒摘要 | `ppt16x9` | `xhs` 3:4 | 摘要是「另一份设计」，不是缩放 |
| 讲义 + 投影 | `a4` | `ppt16x9` | A4 可以密，投影必须疏 |
| 视频 + 封面 | `story` 9:16 | `story` | 封面单独设计，别截视频帧 |
| 报告 + 头图 | `a4` | `wechat` | 头图只留标题一句 |

---

## 六 · 与三条产线的落点

| 线 | 在哪一步定画布 | 怎么改 |
|---|---|---|
| 手作线 | 阶段 B 立约（`design-brief.md` 的 Canvas Specification） | 改 brief 的 Canvas 段，重算字号表 |
| 量产线 | `deck_data.json` 的 `canvas`（`com_drive.py` 会校验 960×540 之外的比例） | 换画布要同时重算坐标与字号 |
| 网页线 | 骨架的 CSS 变量与 `--slide-w/--slide-h`（若骨架支持） | 走 `34-web-tweaks.md` 的 tweak 切换 |
| 原生编辑 | 不动，跟随源文件 | — |
